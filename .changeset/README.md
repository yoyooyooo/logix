# Changesets In This Repo

- `effect-v4` is the active beta release lane.
- Add one changeset for every release-worthy public package change that should reach npm.
- Beta publishes go to the npm `beta` dist-tag through `.github/workflows/release.yml`.
- `main` stays the stable line until the Effect v4 cutover is ready.
