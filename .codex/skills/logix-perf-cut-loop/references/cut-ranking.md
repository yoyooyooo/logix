# Cut Ranking

## 排序顺序

1. P1 门限是否失败
2. 收益是否横向可复用
3. 是否是结构性税
4. 证据是否可信
5. 是否需要 API 对齐

## 推荐优先级

### 先做
- 一刀能让一个 P1 suite 从“全灭”变成“部分通过”
- 一刀能跨多个 suite 收益
- 一刀能把 benchmark 从测错对象改成测对对象

### 后做
- 只改善低档位样本
- 只影响一个诊断档位且量级很小
- 结论高度依赖噪声样本
