# PR #127（O-025）修复闭环记录

- PR: https://github.com/yoyooyooo/logix/pull/127
- 分支: `pr/o025-devtoolshub-tiered-projection`
- 记录时间: 2026-02-27

## 已修复项

1. 修复 `verify` 失败：`Debug.devtoolsHubLayer` 在未显式传入 `diagnosticsLevel` 时不再把默认值误当成显式配置，避免 ring-trim policy 事件污染 `off` 断言。
2. 修复 `projectionTier` 跨 runtime 污染：`DevtoolsHub` 改为按 `runtimeLabel` 解析投影层级，避免不同 runtime 相互降级与清缓存。
3. 修复全局快照一致性：当全局为 `light` 时，不再把 runtime-local `full` 的 `latest*` 写入全局 `latest*`，保证全局 projection 与可见字段一致。
4. 修复 Devtools UI 诊断徽标来源：`DevtoolsShell` 的 projection badge 改为跟随 `selectedRuntime`。
5. 新增回归测试覆盖：
   - runtimeLabel 隔离与无 cross-runtime cache bleed
   - 全局 light 与 runtime-local full 并存一致性
   - mixed runtime 下 projection badge 随选择 runtime 正确切换

## 验证记录（最小闭环）

- `pnpm --filter @logixjs/core exec vitest run test/Debug/Debug.DiagnosticsLevels.test.ts test/Debug/DevtoolsHub.ProjectionTier.test.ts` ✅
- `pnpm --filter @logixjs/devtools-react exec vitest run test/internal/devtools-react.integration.test.tsx` ✅
- `pnpm test:turbo` ✅（14/14 successful）
- `pnpm typecheck` ✅
- `pnpm lint` ✅

## 独立 Review 结论（subagent）

```json
{
  "verdict": "pass",
  "unresolved_medium_high_count": 0,
  "findings": [],
  "gate_checklist": {
    "verify": "pass",
    "tests": "pass",
    "typecheck": "pass",
    "lint": "pass",
    "review_threads_closed": "not_applicable"
  },
  "merge_decision": "ready",
  "merge_reason": "基于当前分支相对 origin/main 的代码变更独立复审，未发现仍未解决的中高风险项；并已通过相关测试、typecheck 与 lint。"
}
```

## Gate Checklist

- [x] Verify（CI 对齐：`pnpm test:turbo`）
- [x] 定向测试（core/devtools-react）
- [x] 独立 review 完成并给出结构化 verdict
- [x] `unresolved_medium_high_count = 0`
- [x] Review Threads：`not_applicable`（当前 PR 无 unresolved review thread）

## Merge Decision

- Decision: **ready**
- Reason: verify 已恢复，review 风险项已修复并有回归测试，独立复审无未解决中高风险。
