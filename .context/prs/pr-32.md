# PR 32: refactor(logix-core): unify selectorgraph core evaluation path

- URL: https://github.com/yoyooyooo/logix/pull/32
- 分支: `refactor/logix-core-selectorgraph-corepath-20260223`
- 状态: CI watcher 监控中（自动合并）

## 核心改动
- `SelectorGraph.onCommit` 抽取共享评估路径：`shouldEvaluateEntryForDirtyRoots` + `evaluateEntry`。
- 预构建 `readRootKeySet`，减少根键命中判断的线性匹配开销。
- 新增多 selector 回归测试，锁定 root-key overlap 语义。

## 本地验证
- `pnpm typecheck`
- `pnpm test:turbo`

## 独立审查
- subagent `019c865e-f79d-7d33-8fb7-9a53d89bb7bf`：无阻塞问题，可合并。
