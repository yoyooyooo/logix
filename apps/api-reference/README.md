# Logix API Reference（自动生成）

本目录用于生成一个**独立的、可当字典查**的 Logix API Reference 静态站点（HTML），签名以 TypeScript 类型为准，避免手写文档漂移。

## 生成

在仓库根目录运行：

```bash
pnpm api-reference:build
```

产物输出到：

- `apps/api-reference/dist/`

