---
title: API Reference（自动生成）
description: 独立的 API Reference 站点入口（签名与导出严格对齐）。
---

这里是 Logix 的 **API Reference（可当字典查）** 入口：它由 TypeScript 类型自动生成，避免手写文档与真实导出漂移。

- 生成命令：`pnpm api-reference:build`
- 产物目录：`apps/api-reference/dist/`
- 本地预览：`pnpm api-reference:serve`
