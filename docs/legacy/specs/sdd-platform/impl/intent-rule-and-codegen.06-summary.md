# 6. 总结

当前主线在 IntentRule ↔ TS 代码映射上的策略是：

- 明确可解析子集（Fluent DSL + 常见 Flow 组合）；
- 生成规范化、结构化的代码写法，方便 Parser 反向还原；
- 利用 ESLint/工具链约束越界写法，降低解析器复杂度；
- 对解析不了的部分坦率标为 Gray/Black Box，而不是做不可靠的“智能猜测”。

这为后续 Full‑Duplex（画布 ↔ 代码双向实时同步）打下稳定基础。***
