# Resume Steps（新会话 2 分钟内恢复）

1. 读 `handoff/00-tldr.md` 与 `handoff/20-decisions.md`，确认硬约束不变。
2. CLI/DevServer 的 M2+ 已达标：打开 `handoff/30-next-actions.md`，确认 vNext 方向后，把优先级切回 `specs/README.md` 的四条跑道。
3. 进入实现时，以目标 spec 的 `specs/*/tasks.md` 为唯一任务清单（先 P0→P1→P2；并行前先约定共享文件 owner）。
