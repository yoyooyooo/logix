# Open Questions（需要进一步裁决/信息）

当前无 blocker 级未决问题（用户已确认 Q-List 裁决）。

可选（实现阶段再固化）：

- CLI cold start `< 500ms` 的“基线环境”如何定义与测量（以便写入可复现门禁）。
- Loader Spy 的“可检测 IO”范围：哪些 IO 视为可检测 violation、哪些只能作为 limitation 明示。

