/**
 * Internal module contract barrel.
 *
 * - Re-exports the normalized runtime-facing module contract.
 * - Public authoring still follows declaration semantics plus a returned run effect.
 */
export * from './runtime/core/module.js'
