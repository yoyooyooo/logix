# references（010-form-api-perf-boundaries）

本目录用于暂存“外部启发/评审备忘”。当某条结论已被 010 的 `spec.md/plan.md/tasks.md/quickstart.md` 固化后，这里应尽量删掉重复叙述，仅保留：

- 后置（DEFER）项：不进入 010 核心交付，但未来可能做；
- 不采纳（REJECT）项：明确不做，避免复议；
- 待定（OPEN）项：需要后续证据/决策才能纳入。

## 当前文件状态

| 文件 | 状态 | 说明 |
| --- | --- | --- |
| `tanstack-form-react.md` | ACTIVE | 仅保留后置（DEFER）/不采纳（REJECT） |
| `pr.md` | ACTIVE | 仅保留待定（OPEN）/后置（DEFER） |

## 已吸收（010 已固化）

已吸收项以 010 的 `spec.md/plan.md/tasks.md/quickstart.md` 为唯一事实源；本目录不再重复维护索引。
