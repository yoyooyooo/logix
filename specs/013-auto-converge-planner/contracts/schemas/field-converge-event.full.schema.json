{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://logix.local/specs/013-auto-converge-planner/schemas/field-converge-event.full.schema.json",
  "title": "TraceEvent (field:converge, full)",
  "description": "A Spec 009 DynamicTrace event whose kind is `field:converge` and level is `full`.",
  "allOf": [
    {
      "$ref": "../../../009-txn-patch-dirtyset/contracts/schemas/dynamic-trace.schema.json#/properties/events/items"
    },
    {
      "type": "object",
      "required": ["txnId", "kind", "data"],
      "properties": {
        "level": { "const": "full" },
        "txnId": { "type": "string", "minLength": 1 },
        "kind": { "const": "field:converge" },
        "data": { "$ref": "./field-converge-data.full.schema.json" }
      }
    }
  ]
}
