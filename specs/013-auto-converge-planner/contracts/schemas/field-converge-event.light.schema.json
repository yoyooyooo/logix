{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://logix.local/specs/013-auto-converge-planner/schemas/field-converge-event.light.schema.json",
  "title": "TraceEvent (field:converge, light)",
  "description": "A Spec 009 DynamicTrace event whose kind is `field:converge` and level is `light`.",
  "allOf": [
    {
      "$ref": "../../../009-txn-patch-dirtyset/contracts/schemas/dynamic-trace.schema.json#/properties/events/items"
    },
    {
      "type": "object",
      "required": ["txnId", "kind", "data"],
      "properties": {
        "level": { "const": "light" },
        "txnId": { "type": "string", "minLength": 1 },
        "kind": { "const": "field:converge" },
        "data": { "$ref": "./field-converge-data.light.schema.json" }
      }
    }
  ]
}
