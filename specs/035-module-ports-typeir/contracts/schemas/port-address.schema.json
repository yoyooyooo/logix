{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://intent-flow.local/specs/035-module-ports-typeir/schemas/port-address.schema.json",
  "title": "PortAddress",
  "oneOf": [
    {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "key"],
      "properties": {
        "kind": { "type": "string", "enum": ["action", "event", "output"] },
        "key": { "type": "string", "minLength": 1 }
      }
    },
    {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "path"],
      "properties": {
        "kind": { "type": "string", "enum": ["export"] },
        "path": { "type": "string", "minLength": 1 }
      }
    }
  ]
}
