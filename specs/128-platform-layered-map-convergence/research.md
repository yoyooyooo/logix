# Research: Platform Layered Map Convergence

## Decision 1: `01-layered-map` 需要 layer-to-code owner 映射

- 否则它只是概念图

## Decision 2: 三条链必须拆开维护

- implementation chain
- governance chain
- host projection chain

## Decision 3: layer boundary 直接写成 code roots + owner spec

- 不再让 `01-layered-map` 停留在纯概念图
- 每层都必须能直接落到 code roots 和 owner spec
