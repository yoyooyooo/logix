# Contracts: Anchor Profile Static Governance

## 1. Static Role Contract

- 只有带 identity / 验证 / 诊断收益的静态角色可保留

## 2. Instantiation Contract

- `Program.make(Module, config)` 与 `Runtime.make(Program)` 仍是唯一公开装配与运行入口

## 3. Naming Contract

- naming bucket 只处理命名问题
- 结构边界继续回 `02`
- 命名重开必须有明确收益或持续误读证据
