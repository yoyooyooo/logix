# Research: Anchor Profile Static Governance

## Decision 1: `02` 与 postponed naming bucket 必须同 spec 裁决

- 结构与命名分离是目标
- 但第二波需要一个 owner 同时看住这两页的边界

## Decision 2: `Workflow` 只允许留在局部原型或历史上下文

- 不重新抬回公开主链

## Decision 3: naming bucket 只记当前口径与重开条件

- 结构是否保留，继续回 `platform/02`
- naming bucket 只记录当前叙事、owner 与重开条件
